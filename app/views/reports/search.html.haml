.contents.row
  = @reports.each do |report|
    =link_to reports_path(@report) do
      .caddy-name 
      = report.name
      = report.bags
.return_btn
  = link_to "caddy情報登録ページへ",new_report_path,class:"teacher-details-edit__btn"
.return_btn
  = link_to "caddy情報検索ページへ",reports_path,class:"teacher-details-edit__btn"
